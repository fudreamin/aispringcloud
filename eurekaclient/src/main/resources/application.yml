server:
  port: 8020
spring:
  application:
    name: provider #微服务名
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  #访问注册中心地址
  instance:
    prefer-ip-address: true